{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://clipforge.dev/schemas/clipforge.json",
  "title": "ClipForgeContracts",
  "description": "The complete ClipForge wire protocol. The PWA and the worker are two independent implementations of the types defined here; both are generated from this file, so neither can drift from it. See docs/adr/0005-single-source-contracts.md. Every timestamp is an ISO 8601 date-time string, NOT a Firestore Timestamp: the store adapters convert at the boundary, which keeps this document language-neutral and lets the unit tier compare documents as plain JSON with no emulator running.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "job": { "$ref": "#/definitions/Job" },
    "jobEvent": { "$ref": "#/definitions/JobEvent" },
    "stage": { "$ref": "#/definitions/Stage" },
    "source": { "$ref": "#/definitions/Source" },
    "ingestErrorCode": { "$ref": "#/definitions/IngestErrorCode" },
    "transcript": { "$ref": "#/definitions/Transcript" },
    "transcriptRef": { "$ref": "#/definitions/TranscriptRef" },
    "candidate": { "$ref": "#/definitions/Candidate" },
    "clip": { "$ref": "#/definitions/Clip" },
    "clipPreview": { "$ref": "#/definitions/ClipPreview" },
    "publication": { "$ref": "#/definitions/Publication" },
    "channel": { "$ref": "#/definitions/Channel" },
    "userProfile": { "$ref": "#/definitions/UserProfile" },
    "workerHeartbeat": { "$ref": "#/definitions/WorkerHeartbeat" },
    "agentReport": { "$ref": "#/definitions/AgentReport" },
    "preference": { "$ref": "#/definitions/Preference" },
    "llmPreferenceProposal": { "$ref": "#/definitions/LlmPreferenceProposal" },
    "remakeOptions": { "$ref": "#/definitions/RemakeOptions" },
    "appliedRemake": { "$ref": "#/definitions/AppliedRemake" },
    "llmRemakeNote": { "$ref": "#/definitions/LlmRemakeNote" },
    "llmClipResponse": { "$ref": "#/definitions/LlmClipResponse" }
  },

  "definitions": {
    "JobStatus": {
      "title": "JobStatus",
      "description": "Lifecycle of a job. Transitions are defined in docs/PLAN.md 3.3 and enforced by clipforge.scheduler.lease.",
      "type": "string",
      "enum": ["QUEUED", "RUNNING", "COMPLETED", "FAILED", "CANCELLED"]
    },

    "JobType": {
      "title": "JobType",
      "description": "ECHO is a no-op job of three artificial stages used to exercise the scheduler without touching media. CLIP is the real pipeline. PUBLISH is a separate, single-stage job created after a human approves a clip — publishing cannot be a stage of CLIP because it happens on the far side of a human decision that may take days. MUSIC is the same shape for the same reason: scoring a finished clip is a choice someone makes while watching it, and it produces a new clip rather than altering the one they watched. UPLOAD is how a reviewer on a phone asks for a clip that only exists on the worker's disk: the phone cannot reach the worker, so the request travels as a job like everything else. REMAKE is the correction channel: a reviewer watching a finished clip says what is wrong with it — the framing lost the ball, the voice has to change — and gets a new clip rather than an edited one, for the same reason MUSIC does.",
      "type": "string",
      "enum": ["ECHO", "CLIP", "PUBLISH", "MUSIC", "UPLOAD", "REMAKE"]
    },

    "StageStatus": {
      "title": "StageStatus",
      "description": "A stage that is DONE is never re-executed on retry. That is what makes a crash during ANALYZE cost seconds rather than the twenty minutes of DOWNLOAD and TRANSCRIBE that preceded it.",
      "type": "string",
      "enum": ["PENDING", "RUNNING", "DONE", "SKIPPED", "FAILED"]
    },

    "StageName": {
      "title": "StageName",
      "description": "Ordered pipeline steps. ECHO_* belong to the ECHO job type only. UPLOAD is the single stage of an UPLOAD job: it copies a clip that already exists on the worker into the bucket so a phone can play it. REMAKE is the single stage of a REMAKE job: it re-cuts a clip from its original source with the reviewer's corrections applied.",
      "type": "string",
      "enum": [
        "ECHO_ONE",
        "ECHO_TWO",
        "ECHO_THREE",
        "DOWNLOAD",
        "TRANSCRIBE",
        "ANALYZE",
        "RENDER",
        "PUBLISH",
        "MUSIC",
        "UPLOAD",
        "REMAKE"
      ]
    },

    "Lane": {
      "title": "Lane",
      "description": "Which scheduler lane a stage runs in. The GPU lane is depth 1 because Whisper and the LLM cannot be co-resident in 6 GB of VRAM (docs/PLAN.md 2.1).",
      "type": "string",
      "enum": ["GPU", "CPU"]
    },

    "StageError": {
      "title": "StageError",
      "description": "Structured failure detail. Captured rather than raised so a failed stage can be inspected from the PWA without reading worker logs.",
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "message"],
      "properties": {
        "type": { "type": "string", "description": "Exception class name." },
        "message": { "type": "string" },
        "code": {
          "type": ["string", "null"],
          "default": null,
          "description": "A stable, machine-readable cause the PWA can map to a written explanation — see IngestErrorCode for the ingest stage's vocabulary. Null when a failure has no classified cause, in which case the UI falls back to `message`. Typed as a string rather than an enum so each stage can own its own vocabulary without every stage's codes leaking into every error."
        },
        "traceback": { "type": ["string", "null"], "default": null },
        "retryable": {
          "type": "boolean",
          "default": true,
          "description": "False marks a failure that will never succeed on retry (bad input, a rights refusal), so the scheduler fails the job immediately instead of burning its remaining attempts."
        }
      }
    },

    "Stage": {
      "title": "Stage",
      "description": "One idempotent, checkpointed step of a job.",
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "lane", "status"],
      "properties": {
        "name": { "$ref": "#/definitions/StageName" },
        "lane": { "$ref": "#/definitions/Lane" },
        "status": { "$ref": "#/definitions/StageStatus" },
        "startedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "endedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "durationMs": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "peakVramMb": {
          "type": ["integer", "null"],
          "minimum": 0,
          "default": null,
          "description": "Peak VRAM observed through NVML while this stage held the ModelBroker lease. Null for CPU stages."
        },
        "attempts": { "type": "integer", "minimum": 0, "default": 0 },
        "checkpoint": {
          "type": ["object", "null"],
          "default": null,
          "additionalProperties": true,
          "description": "Opaque, stage-owned resume state. The scheduler persists and returns it verbatim and never interprets it."
        },
        "error": {
          "anyOf": [{ "$ref": "#/definitions/StageError" }, { "type": "null" }],
          "default": null
        }
      }
    },

    "AppliedMusic": {
      "title": "AppliedMusic",
      "description": "What was actually done to a scored clip, recorded on the clip itself. Provenance rather than configuration: it answers 'what is this version, and where did the track come from' months later, when the job that made it is long gone.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "captions", "source"],
      "properties": {
        "mode": { "$ref": "#/definitions/MusicMode" },
        "captions": { "$ref": "#/definitions/MusicCaptions" },
        "source": {
          "type": "string",
          "description": "The URL or path the track came from, verbatim."
        },
        "trackTitle": {
          "type": ["string", "null"],
          "default": null,
          "description": "What the source called itself, when it said. Worth keeping for the description and for answering a claim."
        },
        "tempoBpm": {
          "type": ["number", "null"],
          "default": null,
          "description": "The tempo the analysis settled on. Null when the track had no beat clear enough to measure, in which case alignment was skipped rather than guessed."
        },
        "musicStartSec": {
          "type": ["number", "null"],
          "default": null,
          "description": "Where in the track the excerpt begins. Rarely 0: a track's first bars are usually its least interesting, so the stage picks a section by energy and starts it on a downbeat."
        },
        "rights": {
          "anyOf": [{ "$ref": "#/definitions/RightsAttestation" }, { "type": "null" }],
          "default": null
        }
      }
    },

    "MusicMode": {
      "title": "MusicMode",
      "description": "What the added track does to the clip's own audio. BED keeps the original and sits the music under it, ducking automatically so speech stays intelligible — the usual choice for a talking clip. REPLACE removes the original audio entirely and the track becomes the whole soundtrack, which is what a montage or a silent-action clip wants.",
      "type": "string",
      "enum": ["BED", "REPLACE"]
    },

    "MusicCaptions": {
      "title": "MusicCaptions",
      "description": "Captions are burned into the clip's pixels by the RENDER stage, so they cannot be peeled off a finished file — removing them means re-rendering the segment from the original source without the subtitle filter. KEEP is therefore cheap and always available; REMOVE needs the source media to still be on the worker, and fails clearly when it has been garbage-collected. Offered as a choice rather than inferred from the mode because 'music over captions' is a real style, and guessing wrong either way is worse than asking.",
      "type": "string",
      "enum": ["KEEP", "REMOVE"]
    },

    "MusicOptions": {
      "title": "MusicOptions",
      "description": "What to add to a clip, and how. Carried on the MUSIC job that produces the scored version.",
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "mode", "captions", "rights"],
      "properties": {
        "source": {
          "type": "string",
          "minLength": 1,
          "description": "A YouTube URL, or a path to an audio file on the worker. Only the audio is ever fetched from a URL — the video of a music source is of no use here and would cost bandwidth for nothing."
        },
        "mode": { "$ref": "#/definitions/MusicMode" },
        "captions": { "$ref": "#/definitions/MusicCaptions" },
        "gainDb": {
          "type": ["number", "null"],
          "minimum": -40,
          "maximum": 12,
          "default": null,
          "description": "Trim on the music, relative to the level the stage picks. Null means 'use the stage's judgement', which targets a bed roughly 18 LUFS below speech and a replacement at the clip's own loudness target."
        },
        "alignToBeat": {
          "type": "boolean",
          "default": true,
          "description": "Whether to start the music excerpt exactly on a beat, so its pulse lands with the clip's first frame. The music moves to meet the clip, never the other way round: re-cutting the video to fall on a beat would mean the published clip differed from the one that was reviewed, and would force a re-encode to achieve something the listener hears identically either way. Ignored when the track has no tempo clear enough to measure."
        },
        "rights": {
          "$ref": "#/definitions/RightsAttestation",
          "description": "Why this track may be used. The same gate the video passes, applied to the music, because a Content ID claim does not care which half of the file it came from. It does not make a claim less likely — it records who decided the track was usable, which is the question that matters afterwards."
        }
      }
    },

    "PreferenceScope": {
      "title": "PreferenceScope",
      "description": "How widely a learned preference applies. SOURCE is this channel or series only \u2014 the right default, because most corrections are about the kind of footage rather than about video in general, and a rule learned from football should not reframe a talking head. EVERYTHING is for a preference that genuinely holds across all of them, which is rarer than it feels while writing one.",
      "type": "string",
      "enum": ["SOURCE", "EVERYTHING"]
    },

    "PreferenceStatus": {
      "title": "PreferenceStatus",
      "description": "PROPOSED until a human says otherwise, and nothing is applied while it sits there. ACCEPTED means it shapes later remakes; REJECTED means it never comes back. Rejected preferences are kept rather than deleted precisely so the same suggestion cannot be made again on the next correction \u2014 that is the difference between a system that learns and one that nags.",
      "type": "string",
      "enum": ["PROPOSED", "ACCEPTED", "REJECTED"]
    },

    "RemakeDefaults": {
      "title": "RemakeDefaults",
      "description": "The machine-readable half of a preference: settings to pre-fill on a future remake. Every field is optional, and a preference may have none at all \u2014 plenty of what a reviewer teaches is a judgement the controls cannot hold (\"this channel's wide shots are unusable cropped\") and is worth carrying as a sentence into the prompt even when it fills in no box.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "framingMode": {
          "anyOf": [{ "$ref": "#/definitions/FramingMode" }, { "type": "null" }],
          "default": null
        },
        "crop": {
          "anyOf": [{ "$ref": "#/definitions/CropAnchor" }, { "type": "null" }],
          "default": null
        },
        "language": {
          "type": ["string", "null"],
          "maxLength": 16,
          "default": null
        },
        "speechMode": {
          "anyOf": [{ "$ref": "#/definitions/SpeechMode" }, { "type": "null" }],
          "default": null
        },
        "obscure": {
          "anyOf": [{ "$ref": "#/definitions/ObscureOptions" }, { "type": "null" }],
          "default": null,
          "description": "Regions to hide on future clips. The one kind of preference whose value is mostly in its coordinates rather than in its sentence: a channel's bug does not move, so the box found once is the box forever, and accepting it is what turns 'blur the canal+' from an instruction into a property of the channel."
        }
      }
    },

    "Preference": {
      "title": "Preference",
      "description": "Something the system noticed it should remember, at preferences/{preferenceId}.\n\nThe point is to stop asking. A reviewer who writes \"follow the ball\" on every football clip is teaching the same thing every time, and a system that cannot hold it makes them type it forever. So after a remake that applied feedback, the local model is asked what \u2014 if anything \u2014 generalises, and the answer is stored here as a proposal.\n\n**Proposed, never applied.** A preference does nothing until a human accepts it. A wrong one is more expensive than a missed one, because it silently shapes every later clip and the reviewer has no reason to suspect it; a missed one costs one more sentence in a note. The model is also shown what has already been accepted AND what has been rejected, so it neither repeats itself nor re-proposes something that was already turned down.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "uid", "scope", "category", "lesson", "status", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "scope": { "$ref": "#/definitions/PreferenceScope" },
        "sourceId": {
          "type": ["string", "null"],
          "default": null,
          "description": "Which source this applies to when the scope is SOURCE. Null for an EVERYTHING preference, which is stored unattached so it is retrieved for every clip."
        },
        "category": { "$ref": "#/definitions/NoteTopic" },
        "lesson": {
          "type": "string",
          "minLength": 1,
          "maxLength": 400,
          "description": "The standing instruction, written for a remake of a clip nobody has seen yet. It must make sense without the clip that prompted it: \"this channel's wide shots lose the ball unless the window follows it\", not \"the framing was wrong on that one\"."
        },
        "defaults": {
          "anyOf": [{ "$ref": "#/definitions/RemakeDefaults" }, { "type": "null" }],
          "default": null
        },
        "status": { "$ref": "#/definitions/PreferenceStatus" },
        "fromClipId": { "type": ["string", "null"], "default": null },
        "fromNote": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "default": null,
          "description": "The feedback that taught it, verbatim. Kept so a preference can be judged against what was actually said rather than against the model's paraphrase of it."
        },
        "timesApplied": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "How many remakes this preference has shaped since it was accepted. The number that says whether it is earning its place: one that never fires is noise, and one that fires constantly is a default the pipeline should probably adopt outright."
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "decidedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "decidedBy": { "type": ["string", "null"], "default": null }
      }
    },

    "LlmPreferenceProposal": {
      "title": "LlmPreferenceProposal",
      "description": "The schema-constrained answer to \"what, if anything, should be remembered from this correction?\". Handed to Ollama as a format constraint like the other LLM shapes here.\n\n`recurring` and `reasoning` come first, and that ordering is the whole design. Constrained decoding emits properties in declaration order, and an empty array satisfies an array schema trivially \u2014 so a model asked only for a list returns `[]` every single time, which it did on all four measured cases including the one that plainly taught two things. Being made to answer a yes/no question and justify it *before* the list is reached turns the same model into one that answers. It is the same lesson `LlmRemakeNote` records: where a schema offers a lazy path, a small model takes it.\n\nAn empty list is still a correct and common answer \u2014 most corrections are about one clip's own moment \u2014 and a wrong standing rule costs far more than a missed one.",
      "type": "object",
      "additionalProperties": false,
      "required": ["recurring", "reasoning", "preferences"],
      "properties": {
        "recurring": {
          "type": "boolean",
          "description": "Would a DIFFERENT clip from this same source be made better if the system already knew something from this correction? Answered first, before any list exists to be left empty."
        },
        "reasoning": {
          "type": "string",
          "maxLength": 300,
          "description": "One sentence on why. Not stored \u2014 it exists to make the model state a position it then has to be consistent with, which is worth more than the tokens it costs."
        },
        "preferences": {
          "type": "array",
          "maxItems": 3,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["lesson", "category", "scope"],
            "properties": {
              "lesson": { "type": "string", "maxLength": 400 },
              "category": { "$ref": "#/definitions/NoteTopic" },
              "scope": { "$ref": "#/definitions/PreferenceScope" },
              "framingMode": {
                "anyOf": [{ "$ref": "#/definitions/NoteFraming" }, { "type": "null" }],
                "default": null
              },
              "language": { "type": ["string", "null"], "maxLength": 16, "default": null }
            }
          }
        }
      }
    },

    "UnsupportedAsk": {
      "title": "UnsupportedAsk",
      "description": "Something a reviewer asked for that ClipForge cannot do. Recorded rather than ignored, because the alternative is what happened in practice: a reviewer asked for a watermark to be removed, got back a clip with the watermark still on it and no explanation, and had no way to tell 'refused' from 'misunderstood' from 'quietly broken'. It also doubles as the list of what to build next, written by the person who wanted it.\n\nREMOVE_WATERMARK and REMOVE_OVERLAY_TEXT are kept as members and are no longer refusals: both are now answered by ObscureOptions, and the note reader routes them to the OBSCURE topic. They stay in the enum because stored readings contain them, and because detection can still come back empty — at which point 'nothing static was found to hide' is the honest refusal and this is what it is recorded as.",
      "type": "string",
      "enum": [
        "REMOVE_WATERMARK",
        "REMOVE_OVERLAY_TEXT",
        "CHANGE_MUSIC",
        "ZOOM_ON_SUBJECT",
        "SLOW_MOTION",
        "REORDER_OR_CUT_MIDDLE",
        "COLOUR_OR_GRADE",
        "SOMETHING_ELSE"
      ]
    },

    "ObscureMethod": {
      "title": "ObscureMethod",
      "description": "How a region of the picture is hidden.\n\nFour, because one is wrong for the two cases that matter. DELOGO reconstructs the area from the pixels around it and is far and away the best answer for a small broadcast bug — at channel-logo size it reads as though the logo was never there. It is also the worst answer for anything large: it has nothing to reconstruct from, so a wide region becomes a smear that draws more attention than the thing it hid. BLUR and PIXELATE stay honest at any size, which is what a burnt-in caption needs. BOX is a flat rectangle, for when honest is the point.",
      "type": "string",
      "enum": ["BLUR", "PIXELATE", "DELOGO", "BOX"]
    },

    "ObscureFound": {
      "title": "ObscureFound",
      "description": "Where a region came from. Worth recording because the three fail differently: AUTO can be in the wrong place, MANUAL cannot but costs the reviewer a drag, and REMEMBERED is a decision made once about a channel and applied ever after — which is the one that needs to be visible when it goes wrong, because nobody asked for it on this clip.",
      "type": "string",
      "enum": ["AUTO", "MANUAL", "REMEMBERED"]
    },

    "ObscureRegion": {
      "title": "ObscureRegion",
      "description": "One rectangle of the SOURCE frame to hide, in percentages of its width and height.\n\nPercentages rather than pixels, and of the source rather than the output, for one reason each. Percentages survive a source that turns out to be 1280 wide when the box was drawn on a 1920 poster. Source coordinates are the only frame a logo is actually fixed in: the output is cropped, panned and scaled, so a box in output coordinates would have to move with the window, and a tracked window would drag the blur across the picture.",
      "type": "object",
      "additionalProperties": false,
      "required": ["xPct", "yPct", "wPct", "hPct"],
      "properties": {
        "xPct": { "type": "number", "minimum": 0, "maximum": 100, "description": "Left edge, as a percentage of source width." },
        "yPct": { "type": "number", "minimum": 0, "maximum": 100, "description": "Top edge, as a percentage of source height." },
        "wPct": { "type": "number", "minimum": 0.2, "maximum": 100 },
        "hPct": { "type": "number", "minimum": 0.2, "maximum": 100 },
        "method": {
          "anyOf": [{ "$ref": "#/definitions/ObscureMethod" }, { "type": "null" }],
          "default": null,
          "description": "Null lets the worker choose by size, which is the better default: DELOGO for a region small enough to reconstruct, BLUR for anything bigger."
        },
        "strength": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 1,
          "default": null,
          "description": "How hard to hide it, 0 to 1. Scales blur radius and pixel size; ignored by DELOGO, which either reconstructs the area or does not. Null means the default, which is strong enough that the shape underneath is not readable."
        },
        "fromSec": {
          "type": ["number", "null"],
          "minimum": 0,
          "default": null,
          "description": "Seconds from the START OF THE CLIP, not of the source, because that is the timebase the render's filters see. Null means from the beginning. For a bug that only appears during play."
        },
        "toSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "label": {
          "type": ["string", "null"],
          "maxLength": 80,
          "default": null,
          "description": "What this is, in a couple of words — 'channel bug, top left'. Shown next to the box in the history, so a remembered region is identifiable a month later."
        },
        "found": {
          "anyOf": [{ "$ref": "#/definitions/ObscureFound" }, { "type": "null" }],
          "default": null
        },
        "confidence": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 1,
          "default": null,
          "description": "For an AUTO region: how static and how distinct it was. Recorded rather than thresholded away, because a low-confidence find that turns out to be right is the evidence for loosening the threshold, and one that is wrong is the evidence for the reviewer to drag the box instead."
        }
      }
    },

    "ObscureOptions": {
      "title": "ObscureOptions",
      "description": "The request to hide things: find them, or hide these, or both.\n\n`auto` and `regions` compose rather than exclude. A reviewer who has drawn one box and also wants the scoreboard found gets both; detection skips anything that overlaps a box already listed, so asking for both never produces two filters over the same pixels.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "auto": {
          "type": "boolean",
          "default": false,
          "description": "Look for static logos and burnt-in text in the footage and hide what is found. Costs a short decode pass over the cut and no model."
        },
        "regions": {
          "type": "array",
          "items": { "$ref": "#/definitions/ObscureRegion" },
          "default": [],
          "maxItems": 6,
          "description": "Rectangles to hide whatever detection thinks. Always applied.\n\nSix, and the number is the security rule's rather than this schema's: each region is a filter pass, and a rule that validates them positionally has a thousand-expression budget that eight of them exceeded. Detection returns at most four."
        },
        "method": {
          "anyOf": [{ "$ref": "#/definitions/ObscureMethod" }, { "type": "null" }],
          "default": null,
          "description": "Override the method for regions that do not name one, found or listed."
        },
        "strength": { "type": ["number", "null"], "minimum": 0, "maximum": 1, "default": null }
      }
    },

    "NoteObscure": {
      "title": "NoteObscure",
      "description": "Whether a note asks for something in the picture to be hidden, and where it says that thing is.\n\nOne enum rather than a boolean plus a location, because the location is only meaningful when the answer is yes and a small model given two fields answers them independently — producing 'no, and it is in the top left'. ANYWHERE is the common answer: reviewers write 'blur the canal+' and expect the system to know where the canal+ is, which is exactly what detection is for. A corner narrows the search and is worth having when they do say.",
      "type": "string",
      "enum": [
        "NOT_MENTIONED",
        "ANYWHERE",
        "TOP_LEFT",
        "TOP_RIGHT",
        "BOTTOM_LEFT",
        "BOTTOM_RIGHT",
        "TOP",
        "BOTTOM"
      ]
    },

    "NoteFraming": {
      "title": "NoteFraming",
      "description": "A framing decision read out of a note, plus the value that means the note did not make one. NOT_MENTIONED is a member rather than the field being nullable, for a reason measured rather than assumed \u2014 see LlmRemakeNote.\n\nPAN is deliberately absent, though FramingMode has it. A pan is defined by its keyframes and this schema gives the model no way to supply any, so a model answering PAN could only ever produce a framing with an empty keyframe list \u2014 which the render path refuses outright, turning a readable note into a failed job. TRACK is the executable form of the same intent: follow the action, with the points worked out from the footage.",
      "type": "string",
      "enum": ["NOT_MENTIONED", "AS_RENDERED", "FIT", "TRACK"]
    },

    "NoteCrop": {
      "title": "NoteCrop",
      "description": "A side read out of a note, plus the value that means the note did not name one.",
      "type": "string",
      "enum": ["NOT_MENTIONED", "centre", "left", "right"]
    },

    "NoteAudio": {
      "title": "NoteAudio",
      "description": "What a note asked for the original audio. REPLACE removes it, KEEP_UNDER keeps it ducked beneath a narration, NOT_MENTIONED means the note said nothing about sound at all \u2014 which is most notes.",
      "type": "string",
      "enum": ["NOT_MENTIONED", "REPLACE", "KEEP_UNDER"]
    },

    "NoteTopic": {
      "title": "NoteTopic",
      "description": "An aspect of a clip a note can be about. Answering this is a much easier question than filling in settings, and it is what bounds the rest of the reading: a field outside the declared topics is ignored, so a model that volunteers a crop for a note about language changes nothing.",
      "type": "string",
      "enum": ["FRAMING", "LANGUAGE", "AUDIO", "TIMING", "OBSCURE"]
    },

    "LlmVisualContext": {
      "title": "LlmVisualContext",
      "description": "What a vision model sees in a handful of frames from one clip.\n\nThis exists because the pipeline was narrating footage it had never looked at. A speech recogniser handed French football commentary returns \"tr\u00e8s mal \u00e0 beaude glim cette frappe pure lat\u00e9rale gauche municois\", a translator faithfully renders that as \"very bad beauty glim this pure left lateral munitions shot\", and a synthesiser reads it aloud over a goal. Every step did its job. Nothing in the chain could tell that the words were nonsense, because nothing in the chain had seen a football.\n\nIt is **description, never inference**. What is in the frame, what the scoreboard says, what colours the teams wear. Not who is about to score.",
      "type": "object",
      "additionalProperties": false,
      "required": ["subject", "happens", "onScreenText"],
      "properties": {
        "subject": {
          "type": "string",
          "maxLength": 200,
          "description": "What kind of footage this is, in a phrase. 'A football match, one side in red, the other in yellow.'"
        },
        "happens": {
          "type": "string",
          "maxLength": 700,
          "description": "What actually occurs across the frames, in order. The part a narration can be built on."
        },
        "onScreenText": {
          "type": "array",
          "items": { "type": "string", "maxLength": 80 },
          "maxItems": 8,
          "description": "Text readable in the frames \u2014 a scoreline, a clock, team abbreviations, hoardings. Worth having separately because it is the one part of a frame that is unambiguous, and because it is often the thing a caption got wrong."
        }
      }
    },

    "LlmNarration": {
      "title": "LlmNarration",
      "description": "A spoken line for one clip, in the language that was asked for.\n\nReplaces \"translate the transcript\", which is correct exactly as often as the transcript is. Handed both the transcript AND what is visible, the model can tell which words survived the recogniser and which did not, and can write around the ones that did not.\n\n`transcriptUsable` comes first and is the whole point of the shape: a model that has committed to an answer about the transcript's quality writes a different script than one that has not been asked. It is the same device `LlmPreferenceProposal` uses, for the same reason \u2014 make the decision before the thing that depends on it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["transcriptUsable", "reasoning", "script"],
      "properties": {
        "transcriptUsable": {
          "type": "boolean",
          "description": "Does the transcript carry meaning a listener would want, or has the recogniser mangled it past rescue? Answered before the script exists."
        },
        "reasoning": {
          "type": "string",
          "maxLength": 300,
          "description": "One sentence on that judgement. Not stored \u2014 it exists to make the model state a position it then has to write consistently with."
        },
        "script": {
          "type": "string",
          "maxLength": 1200,
          "description": "The words to speak, in the target language and nothing else. No speaker labels, no stage directions, no quotation marks around the whole thing: a synthesiser reads whatever is here, literally."
        }
      }
    },

    "LlmClipMetadata": {
      "title": "LlmClipMetadata",
      "description": "A title, a description and tags for one finished clip.\n\nThe fields they replace were never written to be read. `Clip.title` was `Candidate.hook`, which the analysis prompt defines as \"the actual opening line, quoted from the transcript\" \u2014 so a real clip went out titled `\"on a franchi un premier rideau kane peut enroul\u00e9 du plat\"`, lowercase, in French, on a clip that had been re-voiced into English. `Clip.description` was `Candidate.reason`, which is one sentence on *why the clip was selected*: \"This segment captures the high-tension moment where Kane breaks through the defense, creating an immediate visual hook for football fans.\" That is a note from an analyst to a pipeline, and it was being shown to viewers.\n\nWritten for reach rather than for accuracy about the selection: the first few words of a title and the first line of a description are what a feed shows, and they decide whether anything else is read.",
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "tags"],
      "properties": {
        "title": {
          "type": "string",
          "maxLength": 100,
          "description": "The hook, front-loaded, in the target language. What happened and who it happened to, in the first few words, because that is all a feed shows before it truncates."
        },
        "description": {
          "type": "string",
          "maxLength": 900,
          "description": "First line carries the hook again \u2014 it is the only line most viewers see. Then a sentence or two of context, then hashtags on their own line."
        },
        "tags": {
          "type": "array",
          "items": { "type": "string", "maxLength": 40 },
          "minItems": 5,
          "maxItems": 15,
          "description": "Search terms, lowercase, broad ones and specific ones together. `minItems` rather than a request in the prompt, because an empty array satisfies an array schema trivially and a small model takes the cheapest answer that validates \u2014 the lesson LlmPreferenceProposal paid for."
        }
      }
    },

    "LlmRemakeNote": {
      "title": "LlmRemakeNote",
      "description": "The schema-constrained reading of a reviewer's note. Handed to Ollama as a format constraint, like LlmClipResponse, so the model cannot answer with prose.\n\nThree details of this shape are load-bearing, and all three were measured against qwen3.5:4b rather than reasoned about.\n\n**`topics` comes first and bounds everything after it.** Fields belonging to a topic the model did not declare are discarded by the caller. This exists because the two obvious shapes both fail: with nullable optional fields a 4B writes a summary saying it chose TRACK and then emits null for the mode, and with every field required it fills all of them, inventing a crop and a language for a note about timing. Neither a prompt asking for restraint nor one asking for completeness fixes the other. Declaring scope first is a question the model answers reliably, and it makes the scope a property of the protocol rather than a hope.\n\n**Every decision is still required, with 'the note did not say' as a value rather than a null,** so that within a declared topic there is no lazy path that satisfies the schema while deciding nothing.\n\n**`summary` is last.** Constrained decoding emits properties in declaration order, so a model asked to explain itself first explains a decision it has not made yet and then fails to make it.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "topics",
        "framingMode",
        "crop",
        "language",
        "audio",
        "startDeltaSec",
        "endDeltaSec",
        "obscure",
        "summary"
      ],
      "properties": {
        "topics": {
          "type": "array",
          "items": { "$ref": "#/definitions/NoteTopic" },
          "maxItems": 4,
          "description": "Which aspects of the clip this note actually raises. Usually one. An empty array is the correct answer for a note that is a remark rather than an instruction, and the remake then proceeds on whatever was set explicitly."
        },
        "framingMode": { "$ref": "#/definitions/NoteFraming" },
        "crop": { "$ref": "#/definitions/NoteCrop" },
        "language": {
          "type": "string",
          "maxLength": 16,
          "description": "A BCP-47 tag when the note asks for another language, and the literal string NONE when it does not. A required string rather than a nullable one, for the reason given above."
        },
        "audio": { "$ref": "#/definitions/NoteAudio" },
        "startDeltaSec": {
          "type": "number",
          "minimum": -30,
          "maximum": 30,
          "description": "Seconds to move the cut's start. 0 when the note does not say the clip begins at the wrong moment. Negative starts earlier."
        },
        "endDeltaSec": {
          "type": "number",
          "minimum": -30,
          "maximum": 30,
          "description": "Seconds to move the cut's end. 0 when the note does not mention it. Positive runs longer."
        },
        "obscure": { "$ref": "#/definitions/NoteObscure" },
        "unsupported": {
          "type": "array",
          "items": { "$ref": "#/definitions/UnsupportedAsk" },
          "maxItems": 8,
          "description": "Anything the note asked for that none of the controls above can express. Usually empty. Listing something here does not stop the remake \u2014 the rest of the note is still acted on \u2014 it records that one part of the request was understood and cannot be met, which is the difference between a refusal and a silent failure."
        },
        "summary": {
          "type": "string",
          "maxLength": 600,
          "description": "One sentence restating the instruction and naming what was changed. Written last, after the settings it describes.\n\nIt describes this ANSWER, not necessarily the outcome: settings belonging to a topic that was not declared are discarded by the caller afterwards, so a summary can name a change that does not survive. `NoteInterpretation.summary` is rewritten from what was actually applied before it reaches the clip \u2014 an early version recorded the model's own wording and told reviewers it had set a crop it had not."
        }
      }
    },

    "CropAnchor": {
      "title": "CropAnchor",
      "description": "Where a fixed 9:16 window sits in a landscape source. The render profile's own `crop` setting, promoted to the wire so a reviewer can overrule it for one clip without editing a profile that every other clip shares.",
      "type": "string",
      "enum": ["centre", "left", "right"]
    },

    "FramingMode": {
      "title": "FramingMode",
      "description": "How the 9:16 window is decided. AS_RENDERED keeps the profile's fixed crop, which is what every clip got before this existed. FIT crops nothing at all \u2014 the whole landscape frame is scaled into the canvas and the dead space is filled \u2014 so a subject that moves can never leave the picture; the cost is a smaller picture. PAN moves a full-height window along the source over time, between points the reviewer set. TRACK does the same thing but works the points out from the footage, by following where the motion is. FIT and TRACK exist because a fixed crop keeps about a third of a broadcast frame's width and holds still, which is the wrong answer for any sport where the thing worth watching moves.",
      "type": "string",
      "enum": ["AS_RENDERED", "FIT", "PAN", "TRACK"]
    },

    "FitFill": {
      "title": "FitFill",
      "description": "What fills the canvas above and below the picture in FIT mode. BLUR is a scaled, heavily blurred copy of the frame itself, which reads as deliberate and keeps the eye on the centre band. SOLID is a flat colour, which is cheaper to encode and looks better when the footage has a hard horizon the blur would smear.",
      "type": "string",
      "enum": ["BLUR", "SOLID"]
    },

    "PanKeyframe": {
      "title": "PanKeyframe",
      "description": "Where the window sits at one instant. The crop centre is interpolated between consecutive keyframes and held flat outside the first and last, so two points are enough to describe a pan and one is enough to describe a fixed off-centre crop.",
      "type": "object",
      "additionalProperties": false,
      "required": ["atSec", "xPct"],
      "properties": {
        "atSec": {
          "type": "number",
          "minimum": 0,
          "description": "Seconds from the start of the clip, not of the source. A reviewer sets these while watching the clip, and the clip is the only timeline they can see."
        },
        "xPct": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Centre of the window as a percentage of source width. 50 is the middle. Clamped at render time so the window cannot hang off the edge of the frame, which means a keyframe of 0 or 100 is a legal way of saying 'as far left/right as this can go' rather than an error."
        }
      }
    },

    "Framing": {
      "title": "Framing",
      "description": "The reframe half of a remake. Every mode other than AS_RENDERED re-cuts from the original source, because the rendered clip has already had the discarded pixels thrown away \u2014 so these need the source media to still be on the worker, and fail clearly when the workspace collector has taken it.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mode"],
      "properties": {
        "mode": { "$ref": "#/definitions/FramingMode" },
        "crop": {
          "anyOf": [{ "$ref": "#/definitions/CropAnchor" }, { "type": "null" }],
          "default": null,
          "description": "AS_RENDERED only: a fixed anchor overriding the profile's. Null keeps the profile's own."
        },
        "keyframes": {
          "type": "array",
          "items": { "$ref": "#/definitions/PanKeyframe" },
          "default": [],
          "description": "PAN only, and required there: an empty list in PAN mode is a request with no instruction in it. Ignored in every other mode \u2014 TRACK computes its own and records them in the same shape, so a tracked clip can be remade as a PAN with the tracker's work as the starting point."
        },
        "fill": {
          "anyOf": [{ "$ref": "#/definitions/FitFill" }, { "type": "null" }],
          "default": null,
          "description": "FIT only. Null means BLUR."
        },
        "zoom": {
          "type": "number",
          "minimum": 1,
          "maximum": 2,
          "default": 1,
          "description": "How tight the window is. 1 means 'as wide as this mode allows' \u2014 the whole frame in FIT, full source height in PAN and TRACK \u2014 and larger values close in, trading away the margin that keeps a moving subject in shot. The ceiling of 2 is not arbitrary: past it a 1080-line source no longer has the pixels to fill a 1080-wide canvas, and the clip visibly softens."
        },
        "offsetYPct": {
          "type": "number",
          "minimum": -50,
          "maximum": 50,
          "default": 0,
          "description": "FIT only: moves the picture band up or down the canvas, as a percentage of canvas height. Negative is up. Useful when captions want the lower third and the blurred fill above is doing nothing."
        },
        "smoothingSec": {
          "type": "number",
          "minimum": 0,
          "maximum": 10,
          "default": 2,
          "description": "TRACK only: the window of footage the tracker averages over before it moves. Low values follow the action closely and jitter; high values glide and lag. Two seconds is the compromise that survives a camera cut without lurching, and a camera that is itself already following play needs very little on top."
        },
        "maxPanPctPerSec": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "default": 12,
          "description": "TRACK only: a ceiling on how fast the window may travel, in percent of source width per second. This is what stops the crop snapping across the pitch when the motion centroid jumps to a different part of the frame \u2014 the picture lags the ball for a moment, which looks far better than a whip-pan that arrives before anything happens."
        }
      }
    },

    "SpeechMode": {
      "title": "SpeechMode",
      "description": "What a generated voice does to the clip's own audio. REPLACE removes the original entirely, which is the point when the original is commentary you cannot use. BED keeps the original underneath, ducked, which suits footage whose crowd noise and ball contact are half of why the clip works.",
      "type": "string",
      "enum": ["REPLACE", "BED"]
    },

    "VoiceCaptions": {
      "title": "VoiceCaptions",
      "description": "Captions are burned into pixels, so a new voice makes the existing ones wrong \u2014 they show words nobody is saying any more. REBUILD transcribes the generated narration and re-cuts captions from what was actually said rather than from the script, which is the only version that stays in sync when the synthesiser stresses a sentence differently from the way it was written. KEEP is for a bed under unchanged speech. REMOVE leaves the picture clean.",
      "type": "string",
      "enum": ["REBUILD", "KEEP", "REMOVE"]
    },

    "VoiceOptions": {
      "title": "VoiceOptions",
      "description": "A new narration for a clip: what to say, in which language, in whose voice. Worth being plain about the limit of this, because it is easy to reach for the wrong reason: re-voicing changes the soundtrack and nothing else. On third-party footage the picture is still the picture, and it is the picture a rights holder's matching runs against. This helps with a claim on commentary or music, and it opens a clip to an audience that does not speak the original language. It does not make footage safe to publish \u2014 that is what the rights attestation is for.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "voice", "language"],
      "properties": {
        "mode": { "$ref": "#/definitions/SpeechMode" },
        "voice": {
          "type": "string",
          "minLength": 1,
          "description": "The synthesiser's own name for a voice. Opaque here on purpose: this contract should not have to be reissued every time an engine ships a new one."
        },
        "language": {
          "type": "string",
          "minLength": 2,
          "maxLength": 16,
          "description": "The language to speak in, as a BCP-47 tag. Not necessarily the language the clip is in \u2014 that difference is the whole point of `translate`."
        },
        "translate": {
          "type": "boolean",
          "default": true,
          "description": "Whether to translate the clip's words before speaking them. Only meaningful when `language` differs from the source's; when it does and this is false, the synthesiser is being asked to read one language with another's phonetics, which produces something no listener wants."
        },
        "script": {
          "type": ["string", "null"],
          "maxLength": 4000,
          "default": null,
          "description": "What to say, written by hand. Null means 'say what the clip says', which is the ordinary case. A script overrides both the transcript and any translation of it \u2014 the reviewer has stated the words, so nothing else gets to."
        },
        "speed": {
          "type": "number",
          "minimum": 0.5,
          "maximum": 2,
          "default": 1,
          "description": "Playback rate of the synthesised speech. It moves independently of the picture: narration that runs long is not allowed to stretch the video, so the stage reports an overrun rather than silently retiming footage the reviewer already approved."
        },
        "gainDb": {
          "type": ["number", "null"],
          "minimum": -40,
          "maximum": 12,
          "default": null,
          "description": "Trim on the narration. Null means the stage's own judgement, which targets the clip's loudness normalisation rather than a fixed level."
        },
        "duckDb": {
          "type": ["number", "null"],
          "minimum": -40,
          "maximum": 0,
          "default": null,
          "description": "BED only: how far the original audio is pushed down under the narration. Null means -12 dB, which keeps crowd noise present without competing with a voice."
        },
        "captions": {
          "$ref": "#/definitions/VoiceCaptions",
          "description": "What happens to the burned-in captions, which the new voice has otherwise made wrong."
        }
      }
    },

    "RemakeOptions": {
      "title": "RemakeOptions",
      "description": "A reviewer's corrections to a finished clip. Every field is optional because a remake is usually one complaint, not a rebuild: 'the framing lost the ball' and 'this needs to be in Spanish' are separate errands and should not have to be sent together.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "notes": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "default": null,
          "description": "What is wrong with the clip, in the reviewer's own words. Always recorded on the new clip, whether or not anything is inferred from it \u2014 a remake whose result is still wrong is much easier to reason about when what was asked for is written next to what was done."
        },
        "interpretNotes": {
          "type": "boolean",
          "default": true,
          "description": "Whether to put the notes to the local model and let it fill in the options the reviewer left unset. Bounded deliberately: it may choose a framing mode, a crop anchor, a language and a voice, and it may not touch anything the reviewer stated explicitly. What it decided is recorded on the clip, so a note that was misread is visible as a misreading rather than as an unexplained result."
        },
        "framing": {
          "anyOf": [{ "$ref": "#/definitions/Framing" }, { "type": "null" }],
          "default": null,
          "description": "Null means 'leave the framing alone', which is not the same as AS_RENDERED: an explicit AS_RENDERED with a `crop` set is a reframe to a different fixed anchor."
        },
        "voice": {
          "anyOf": [{ "$ref": "#/definitions/VoiceOptions" }, { "type": "null" }],
          "default": null,
          "description": "Null keeps the clip's own audio."
        },
        "startDeltaSec": {
          "type": "number",
          "minimum": -30,
          "maximum": 30,
          "default": 0,
          "description": "Nudge the cut's start, in seconds, relative to where the candidate put it. Negative starts earlier. Boundary snapping gets the sentence right and still lands a beat late for an action clip, where the interesting thing happens before anyone says anything about it."
        },
        "endDeltaSec": {
          "type": "number",
          "minimum": -30,
          "maximum": 30,
          "default": 0,
          "description": "Nudge the cut's end. Positive runs longer."
        },
        "obscure": {
          "anyOf": [{ "$ref": "#/definitions/ObscureOptions" }, { "type": "null" }],
          "default": null,
          "description": "Hide a fixed part of the picture — a channel bug, a scoreboard, a burnt-in caption. Null asks for nothing; an object with `auto` false and no regions is also nothing, and is what an untouched control sends."
        },
        "profile": {
          "type": ["string", "null"],
          "default": null,
          "description": "Render with a different named profile \u2014 caption size, bitrate, the rest of the look. Null keeps the one the clip was made with, which is what makes a reframe comparable to the version it replaces."
        }
      }
    },

    "NoteInterpretation": {
      "title": "NoteInterpretation",
      "description": "What the local model made of the reviewer's note, recorded whether or not it was any use. A remake that came out wrong is nearly always one of two failures \u2014 the note was misread, or it was read correctly and the machinery did the wrong thing \u2014 and without this they are indistinguishable.",
      "type": "object",
      "additionalProperties": false,
      "required": ["understood", "summary"],
      "properties": {
        "understood": {
          "type": "boolean",
          "description": "False when the model could not turn the note into anything actionable. The remake still runs on whatever the reviewer set explicitly, rather than failing: a note nobody could parse is not a reason to refuse work that was otherwise fully specified."
        },
        "summary": {
          "type": "string",
          "maxLength": 600,
          "description": "The instruction as the model understood it, in one sentence, and the settings it changed."
        },
        "model": { "type": ["string", "null"], "default": null }
      }
    },

    "AppliedVoice": {
      "title": "AppliedVoice",
      "description": "The narration that was actually produced. Records the spoken text rather than the script, because those differ whenever a translation happened, and the spoken text is the one a caption has to match and a viewer will hear.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "voice", "language", "engine"],
      "properties": {
        "mode": { "$ref": "#/definitions/SpeechMode" },
        "voice": { "type": "string" },
        "language": { "type": "string" },
        "engine": {
          "type": "string",
          "description": "Which synthesiser, and which version of it. Two voices of the same name from different engines do not sound alike."
        },
        "translated": { "type": "boolean", "default": false },
        "spokenText": {
          "type": ["string", "null"],
          "maxLength": 4000,
          "default": null
        },
        "speechDurationSec": {
          "type": ["number", "null"],
          "minimum": 0,
          "default": null,
          "description": "How long the narration runs. Compared against the clip's own duration by the UI: narration that overruns the picture is the most common way a translated clip goes wrong, and it is invisible until someone watches the end."
        }
      }
    },

    "AppliedRemake": {
      "title": "AppliedRemake",
      "description": "What was asked for, and what was done. Carried on the clip the remake produced, beside `derivedFromClipId`, so the pair reads as a correction and its result rather than as two unrelated clips.",
      "type": "object",
      "additionalProperties": false,
      "required": ["framingMode", "startSec", "endSec"],
      "properties": {
        "notes": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "default": null,
          "description": "The reviewer's words, verbatim."
        },
        "interpretation": {
          "anyOf": [{ "$ref": "#/definitions/NoteInterpretation" }, { "type": "null" }],
          "default": null
        },
        "framingMode": { "$ref": "#/definitions/FramingMode" },
        "keyframes": {
          "type": "array",
          "items": { "$ref": "#/definitions/PanKeyframe" },
          "default": [],
          "description": "The window's path through the source, as rendered \u2014 the reviewer's own points in PAN, and the tracker's findings in TRACK. Kept because it is the only way to see what a tracked remake actually decided to follow, and because a PAN remake can be seeded from it and corrected by hand when it followed the wrong thing."
        },
        "voice": {
          "anyOf": [{ "$ref": "#/definitions/AppliedVoice" }, { "type": "null" }],
          "default": null
        },
        "refusals": {
          "type": "array",
          "items": { "type": "string", "maxLength": 300 },
          "default": [],
          "maxItems": 8,
          "description": "Parts of the request that were understood and NOT carried out, each phrased for the person who asked. A remake that silently does four of the five things asked of it is indistinguishable from one that is broken, and the reviewer's next move \u2014 ask again, ask differently, give up \u2014 depends entirely on which it was."
        },
        "warnings": {
          "type": "array",
          "items": { "type": "string", "maxLength": 300 },
          "default": [],
          "maxItems": 8,
          "description": "Things that were done but are likely to disappoint: a narration built from a transcript the recogniser was unsure of, a translation that barely changed the text, a clip with almost no speech in it. Surfaced next to the result because every one of these has produced a clip that looked finished and was unusable."
        },
        "obscured": {
          "type": "array",
          "items": { "$ref": "#/definitions/ObscureRegion" },
          "default": [],
          "maxItems": 6,
          "description": "The regions actually hidden, with where each came from. This is the record that makes a wrong box fixable: a reviewer who can see that detection put the rectangle two percent too high can drag it and remake, rather than describing the error in prose to a model that will guess again."
        },
        "startSec": {
          "type": "number",
          "minimum": 0,
          "description": "The window actually cut from the source, after any nudge. Absolute source seconds, matching Candidate."
        },
        "endSec": { "type": "number", "minimum": 0 }
      }
    },

    "Job": {
      "title": "Job",
      "description": "One pipeline run over one source. Stored at jobs/{jobId}.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "uid",
        "type",
        "status",
        "stages",
        "attempts",
        "maxAttempts",
        "createdAt",
        "updatedAt"
      ],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": {
          "type": "string",
          "minLength": 1,
          "description": "Owning user. Every security rule keys off this field."
        },
        "type": { "$ref": "#/definitions/JobType" },
        "status": { "$ref": "#/definitions/JobStatus" },
        "sourceId": {
          "type": ["string", "null"],
          "default": null,
          "description": "Set by the DOWNLOAD stage once ingestion resolves the submission to a source. Null until then."
        },
        "submission": {
          "type": ["string", "null"],
          "default": null,
          "description": "What the user actually submitted: a YouTube URL or a local file path. Kept verbatim and separate from sourceId, because a job must be re-runnable from the original input even if its source document was garbage-collected."
        },
        "clipId": {
          "type": ["string", "null"],
          "default": null,
          "description": "The clip a PUBLISH or MUSIC job acts on. Null for every other job type. Security rules read this to check the clip's rights attestation before allowing the job to be created at all."
        },
        "musicOptions": {
          "anyOf": [{ "$ref": "#/definitions/MusicOptions" }, { "type": "null" }],
          "default": null,
          "description": "What a MUSIC job should add, and how. Null for every other job type."
        },
        "remakeOptions": {
          "anyOf": [{ "$ref": "#/definitions/RemakeOptions" }, { "type": "null" }],
          "default": null,
          "description": "What a REMAKE job should correct. Null for every other job type."
        },
        "publishOptions": {
          "anyOf": [{ "$ref": "#/definitions/PublishOptions" }, { "type": "null" }],
          "default": null,
          "description": "Set by the client on a PUBLISH job. Null for every other job type, and null here means 'use the channel defaults'."
        },
        "notBefore": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "The job is not claimable until this instant. Null means claimable immediately. This is how a publish-at time is honoured: scheduling lives in the one predicate every claim path already consults, rather than in a second scheduler that could disagree with the first."
        },
        "stages": {
          "type": "array",
          "items": { "$ref": "#/definitions/Stage" },
          "minItems": 1,
          "description": "Ordered. Executed front to back; DONE stages are skipped on retry."
        },
        "workerId": { "type": ["string", "null"], "default": null },
        "leaseExpiresAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "Set on claim, extended by heartbeat. A RUNNING job whose lease is in the past is reclaimable by the reaper."
        },
        "attempts": { "type": "integer", "minimum": 0, "default": 0 },
        "maxAttempts": { "type": "integer", "minimum": 1, "default": 3 },
        "error": {
          "anyOf": [{ "$ref": "#/definitions/StageError" }, { "type": "null" }],
          "default": null,
          "description": "The failure that terminated the job, copied from the failing stage."
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "startedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "endedAt": { "type": ["string", "null"], "format": "date-time", "default": null }
      }
    },

    "JobEventKind": {
      "title": "JobEventKind",
      "type": "string",
      "enum": [
        "CREATED",
        "CLAIMED",
        "STAGE_STARTED",
        "STAGE_COMPLETED",
        "STAGE_FAILED",
        "COMPLETED",
        "FAILED",
        "CANCELLED",
        "LEASE_EXPIRED",
        "REQUEUED"
      ]
    },

    "JobEvent": {
      "title": "JobEvent",
      "description": "Append-only transition log at jobs/{jobId}/events/{eventId}. Never mutated, which is what makes it trustworthy when debugging a job that failed hours ago.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "jobId", "kind", "at", "seq"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "jobId": { "type": "string", "minLength": 1 },
        "kind": { "$ref": "#/definitions/JobEventKind" },
        "at": { "type": "string", "format": "date-time" },
        "seq": {
          "type": "integer",
          "minimum": 0,
          "description": "Order within a single transition. One transition can emit several events at the identical instant — a reap emits LEASE_EXPIRED and REQUEUED together — and ordering by timestamp alone would then fall back to document id, which is random. The log is ordered by (at, seq)."
        },
        "stage": {
          "anyOf": [{ "$ref": "#/definitions/StageName" }, { "type": "null" }],
          "default": null
        },
        "workerId": { "type": ["string", "null"], "default": null },
        "detail": { "type": ["string", "null"], "default": null },
        "attempts": { "type": ["integer", "null"], "minimum": 0, "default": null }
      }
    },

    "IngestErrorCode": {
      "title": "IngestErrorCode",
      "description": "Why an ingest failed, in terms a user can act on. Phase 3 requires each of these to map to a distinct user-facing message rather than a stack trace: 'this video is age-restricted' is actionable, 'DownloadError' is not. Only RATE_LIMITED and NETWORK are worth retrying.",
      "type": "string",
      "enum": [
        "UNSUPPORTED_URL",
        "NOT_FOUND",
        "PRIVATE",
        "GEO_BLOCKED",
        "AGE_RESTRICTED",
        "LIVE_STREAM",
        "TOO_LONG",
        "NO_SUITABLE_FORMAT",
        "RATE_LIMITED",
        "NETWORK",
        "DISK_FULL",
        "UNKNOWN"
      ]
    },

    "SourceProvider": {
      "title": "SourceProvider",
      "description": "local exists so the whole pipeline can be exercised from a committed fixture with no network, which is what keeps the integration tier runnable in CI.",
      "type": "string",
      "enum": ["youtube", "local"]
    },

    "Source": {
      "title": "Source",
      "description": "One ingested long-form video at sources/{sourceId}. The media itself never leaves the worker (docs/PLAN.md decision D3).",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "uid", "provider", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "provider": { "$ref": "#/definitions/SourceProvider" },
        "externalId": {
          "type": ["string", "null"],
          "default": null,
          "description": "YouTube video id, or null for a local file."
        },
        "url": { "type": ["string", "null"], "default": null },
        "title": { "type": ["string", "null"], "default": null },
        "channel": { "type": ["string", "null"], "default": null },
        "durationSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "contentHash": {
          "type": ["string", "null"],
          "default": null,
          "description": "Hash of the downloaded media. Transcripts are cached against this, so re-ingesting the same video costs no GPU time."
        },
        "localPath": {
          "type": ["string", "null"],
          "default": null,
          "description": "Worker-local absolute path. Advisory only for the PWA, which can never read it."
        },
        "sizeBytes": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "obscure": {
          "anyOf": [{ "$ref": "#/definitions/ObscureOptions" }, { "type": "null" }],
          "default": null,
          "description": "Hidden on every clip cut from this source, including the first. Set by accepting a learned preference, or from the remake form's 'always do this for this channel'.\n\nThis is the difference between a feature and a chore. A channel bug is a property of the channel: found once on one clip, it is in the same place on every clip that channel will ever produce, and a reviewer who has to ask for it each time is doing the system's bookkeeping. Applied at RENDER, so a clip arrives for review already clean rather than arriving wrong and needing a correction."
        },
        "pinned": {
          "type": "boolean",
          "default": false,
          "description": "Exempt from workspace garbage collection. Sources are large and the disk is finite, so GC is not optional — pinning is the escape hatch for one you are still working with."
        },
        "lastAccessedAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "Drives least-recently-used eviction. Touched whenever a stage reads the file, not when the document is read."
        },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "TranscriptWord": {
      "title": "TranscriptWord",
      "description": "Word-level timing. Load-bearing for boundary snapping (D4) and caption generation, which is why faster-whisper is configured with word timestamps enabled.",
      "type": "object",
      "additionalProperties": false,
      "required": ["text", "startSec", "endSec"],
      "properties": {
        "text": { "type": "string" },
        "startSec": { "type": "number", "minimum": 0 },
        "endSec": { "type": "number", "minimum": 0 },
        "probability": { "type": ["number", "null"], "minimum": 0, "maximum": 1, "default": null }
      }
    },

    "TranscriptSegment": {
      "title": "TranscriptSegment",
      "type": "object",
      "additionalProperties": false,
      "required": ["index", "text", "startSec", "endSec"],
      "properties": {
        "index": { "type": "integer", "minimum": 0 },
        "text": { "type": "string" },
        "startSec": { "type": "number", "minimum": 0 },
        "endSec": { "type": "number", "minimum": 0 },
        "words": {
          "type": "array",
          "items": { "$ref": "#/definitions/TranscriptWord" },
          "default": []
        }
      }
    },

    "Transcript": {
      "title": "Transcript",
      "description": "Stored at sources/{sourceId}/transcripts/{modelVersion}, keyed by model so re-transcribing with a better model does not destroy the old one.",
      "type": "object",
      "additionalProperties": false,
      "required": ["sourceId", "modelVersion", "segments", "createdAt"],
      "properties": {
        "sourceId": { "type": "string", "minLength": 1 },
        "modelVersion": {
          "type": "string",
          "minLength": 1,
          "description": "For example faster-whisper:large-v3-turbo:int8_float16"
        },
        "language": { "type": ["string", "null"], "default": null },
        "durationSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "segments": { "type": "array", "items": { "$ref": "#/definitions/TranscriptSegment" } },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "SubScores": {
      "title": "SubScores",
      "description": "The rubric from docs/PLAN.md. The LLM returns these; Python computes the weighted total (decision D5), so the weighting can be changed and every historical candidate rescored without re-running inference.",
      "type": "object",
      "additionalProperties": false,
      "required": ["hook", "curiosity", "standalone", "emotion", "pacing", "shareability"],
      "properties": {
        "hook": { "type": "integer", "minimum": 0, "maximum": 25 },
        "curiosity": { "type": "integer", "minimum": 0, "maximum": 20 },
        "standalone": { "type": "integer", "minimum": 0, "maximum": 20 },
        "emotion": { "type": "integer", "minimum": 0, "maximum": 15 },
        "pacing": { "type": "integer", "minimum": 0, "maximum": 10 },
        "shareability": { "type": "integer", "minimum": 0, "maximum": 10 }
      }
    },

    "Candidate": {
      "title": "Candidate",
      "description": "An LLM-proposed clip window at candidates/{candidateId}, after deterministic boundary snapping.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "uid", "sourceId", "startSec", "endSec", "subScores", "total", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "sourceId": { "type": "string", "minLength": 1 },
        "jobId": { "type": ["string", "null"], "default": null },
        "startSec": {
          "type": "number",
          "minimum": 0,
          "description": "Snapped to a silence boundary and a sentence end, not the raw LLM value."
        },
        "endSec": { "type": "number", "minimum": 0 },
        "proposedStartSec": {
          "type": ["number", "null"],
          "minimum": 0,
          "default": null,
          "description": "The model's raw suggestion, retained so snapping can be evaluated against it."
        },
        "proposedEndSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "subScores": { "$ref": "#/definitions/SubScores" },
        "total": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "Computed in Python from subScores. Never supplied by the model."
        },
        "hook": { "type": ["string", "null"], "default": null },
        "reason": { "type": ["string", "null"], "default": null },
        "transcriptExcerpt": { "type": ["string", "null"], "default": null },
        "modelVersion": { "type": ["string", "null"], "default": null },
        "promptVersion": { "type": ["string", "null"], "default": null },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "ReviewState": {
      "title": "ReviewState",
      "type": "string",
      "enum": ["PENDING", "APPROVED", "REJECTED"]
    },

    "RightsBasis": {
      "title": "RightsBasis",
      "description": "Why the operator believes they may publish this clip. Publishing is disabled by default and no clip can be published without one of these recorded, together with who attested it and when. A null `rights` block means no attestation exists — which is a different thing from a weak one, and the gate refuses it. See docs/PLAN.md Phase 8.",
      "type": "string",
      "enum": [
        "OWN_CONTENT",
        "LICENSED",
        "PERMISSION_GRANTED",
        "FAIR_USE_ASSERTED",
        "PUBLIC_DOMAIN"
      ]
    },

    "ChannelConnection": {
      "title": "ChannelConnection",
      "description": "Whether the worker can currently publish to this channel. NOT_CONFIGURED means no OAuth client has been supplied; NEEDS_AUTH means one has but nobody has authorised it, or the refresh token expired — which it does every 7 days while the consent screen is in Testing mode.",
      "type": "string",
      "enum": ["NOT_CONFIGURED", "NEEDS_AUTH", "CONNECTED", "ERROR"]
    },

    "PublishDefaults": {
      "title": "PublishDefaults",
      "description": "What a publish uses when the clip does not say otherwise. Editable from the app because none of it is secret — unlike the credentials, which never leave the worker.",
      "type": "object",
      "additionalProperties": false,
      "required": ["privacy"],
      "properties": {
        "privacy": { "$ref": "#/definitions/PublishPrivacy" },
        "categoryId": {
          "type": "string",
          "default": "22",
          "description": "YouTube category id. 22 is People & Blogs, which is the safe default for talking-head clips."
        },
        "tags": { "type": "array", "items": { "type": "string" }, "default": [] },
        "titleSuffix": {
          "type": ["string", "null"],
          "default": null,
          "description": "Appended to every title, for a channel handle or series marker. Truncation still applies: YouTube rejects titles over 100 characters."
        },
        "descriptionTemplate": {
          "type": ["string", "null"],
          "default": null,
          "description": "Appended to every description. Where a standing credit, licence note or link block belongs."
        }
      }
    },

    "Channel": {
      "title": "Channel",
      "description": "A publishing destination, at channels/{channelId}. Modelled as a collection from the outset even though the UI manages one: adding a second channel is then a document and a second `youtube-auth`, not a schema migration and a rewrite of every publication record. Deliberately holds NO credentials — the client secret and refresh token live on the worker (docs/adr/0010-worker-held-publishing-credentials.md).",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "uid", "platform", "label", "connection", "defaults", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "platform": { "$ref": "#/definitions/PublishPlatform" },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "What the operator calls it. Free text, because 'the cooking one' is more useful at 6am than a channel id."
        },
        "isDefault": { "type": "boolean", "default": false },
        "externalChannelId": { "type": ["string", "null"], "default": null },
        "externalChannelTitle": {
          "type": ["string", "null"],
          "default": null,
          "description": "Read back from YouTube after authorising, so the app can show which account was actually connected rather than which one was intended."
        },
        "connection": { "$ref": "#/definitions/ChannelConnection" },
        "connectionMessage": {
          "type": ["string", "null"],
          "default": null,
          "description": "Why the connection is not CONNECTED, in words the operator can act on."
        },
        "authorisedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "checkedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "defaults": { "$ref": "#/definitions/PublishDefaults" },
        "quotaDay": { "type": ["string", "null"], "default": null },
        "quotaUsedUnits": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "uploadsRemainingToday": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": ["string", "null"], "format": "date-time", "default": null }
      }
    },

    "PublishOptions": {
      "title": "PublishOptions",
      "description": "What the operator chose for one specific upload, set when the publish is requested. Absent fields fall back to the channel's defaults, so a clip published without opening any of this still behaves sensibly.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "channelId": { "type": ["string", "null"], "default": null },
        "title": {
          "type": ["string", "null"],
          "maxLength": 100,
          "default": null,
          "description": "Replaces the clip's title for this upload only. Capped at YouTube's own limit here rather than truncated silently on the way out, so what the operator typed is what the audit record shows."
        },
        "description": {
          "type": ["string", "null"],
          "maxLength": 4900,
          "default": null,
          "description": "Replaces the clip's description for this upload only."
        },
        "privacy": {
          "anyOf": [{ "$ref": "#/definitions/PublishPrivacy" }, { "type": "null" }],
          "default": null
        },
        "categoryId": { "type": ["string", "null"], "default": null },
        "tags": {
          "type": ["array", "null"],
          "items": { "type": "string" },
          "default": null,
          "description": "Null and empty are different answers here, and both are ones an operator can mean. Null is 'I did not touch the tags', which falls through to the channel's; an empty list is 'no tags on this one', which does not. Every other field in this block is nullable for the same reason, and an array whose absent value was [] could not express the second."
        }
      }
    },

    "UserRole": {
      "title": "UserRole",
      "description": "ADMIN can approve other users and change roles. MEMBER can use the app for their own data and nothing else. There is no third level because there is no third thing to protect.",
      "type": "string",
      "enum": ["ADMIN", "MEMBER"]
    },

    "UserStatus": {
      "title": "UserStatus",
      "description": "Registration is open; access is not. A new account lands in PENDING and can read nothing until an admin approves it, so an unapproved sign-up is an inert row rather than a foothold. REJECTED and DISABLED are kept apart deliberately: one was never let in, the other was and had it taken away, and an audit that cannot tell them apart is not much of an audit.",
      "type": "string",
      "enum": ["PENDING", "APPROVED", "REJECTED", "DISABLED"]
    },

    "UserProfile": {
      "title": "UserProfile",
      "description": "A person with an account, at users/{uid}. The document id IS the Firebase Auth uid, which is what lets security rules resolve a caller's status with one get() and no join.",
      "type": "object",
      "additionalProperties": false,
      "required": ["uid", "email", "role", "status", "createdAt"],
      "properties": {
        "uid": { "type": "string", "minLength": 1 },
        "email": { "type": "string", "minLength": 1 },
        "displayName": { "type": ["string", "null"], "default": null },
        "photoUrl": { "type": ["string", "null"], "default": null },
        "role": { "$ref": "#/definitions/UserRole" },
        "status": { "$ref": "#/definitions/UserStatus" },
        "createdAt": { "type": "string", "format": "date-time" },
        "decidedAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "When an admin last approved, rejected or disabled this account."
        },
        "decidedBy": {
          "type": ["string", "null"],
          "default": null,
          "description": "The uid of the admin who made that decision. Recorded so 'who let this person in?' is answerable later."
        }
      }
    },

    "RightsAttestation": {
      "title": "RightsAttestation",
      "type": "object",
      "additionalProperties": false,
      "required": ["basis"],
      "properties": {
        "basis": { "$ref": "#/definitions/RightsBasis" },
        "attestedBy": { "type": ["string", "null"], "default": null },
        "attestedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "note": { "type": ["string", "null"], "default": null }
      }
    },

    "ClipLocation": {
      "title": "ClipLocation",
      "description": "Where the authoritative copy of a rendered clip lives. LOCAL is the free-tier default: Cloud Storage for Firebase requires the Blaze plan, so on Spark there is no bucket at all and clips stay on the worker. REMOTE means a copy exists that any browser can fetch. See docs/adr/0009-spark-tier-local-artefacts.md.",
      "type": "string",
      "enum": ["LOCAL", "REMOTE"]
    },

    "Clip": {
      "title": "Clip",
      "description": "A rendered artefact at clips/{clipId}. The FILE itself never enters Firestore. On the free tier it stays on the worker and is reachable through the worker's local file server; when Blaze is available the same document also carries a playbackUrl. Both states are first-class here on purpose, so enabling Blaze populates a field rather than migrating a model.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "uid", "candidateId", "location", "localPath", "review", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "candidateId": { "type": "string", "minLength": 1 },
        "sourceId": { "type": ["string", "null"], "default": null },
        "jobId": { "type": ["string", "null"], "default": null },
        "location": { "$ref": "#/definitions/ClipLocation" },
        "localPath": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute path on the worker. Always set, even once a remote copy exists — the worker still needs it to publish (decision D7)."
        },
        "playbackUrl": {
          "type": ["string", "null"],
          "default": null,
          "description": "A URL any browser can fetch. Null on the free tier. Populated by the Cloud Storage adapter, and equally by a tunnel — this field is 'a URL', not 'a Firebase thing'."
        },
        "storagePath": {
          "type": ["string", "null"],
          "default": null,
          "description": "Object path within the bucket, when one exists. Kept alongside playbackUrl because deletion and rules key off the path, not the URL."
        },
        "playbackExpiresAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "When the bucket copy stops being playable. Written at upload as upload time plus the retention window, and honoured by the UI without asking the bucket: the object is removed by a Cloud Storage lifecycle rule, which reports to nobody, so a clip whose expiry has passed is treated as local-only rather than discovered to be missing when someone presses play. Null when there is no bucket copy."
        },
        "thumbnailPath": { "type": ["string", "null"], "default": null },
        "durationSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "widthPx": { "type": ["integer", "null"], "minimum": 1, "default": null },
        "heightPx": { "type": ["integer", "null"], "minimum": 1, "default": null },
        "sizeBytes": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "renderProfile": { "type": ["string", "null"], "default": null },
        "title": {
          "type": ["string", "null"],
          "maxLength": 200,
          "default": null,
          "description": "What this clip is called, written to be read. Until LlmClipMetadata existed this was `Candidate.hook` — a line quoted out of the transcript — so clips went out titled with lowercase French ASR fragments."
        },
        "description": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "default": null,
          "description": "The text that goes out with the clip. Previously `Candidate.reason`, which is one sentence on why the window was SELECTED — an analyst's note to a pipeline, shown to viewers."
        },
        "tags": {
          "type": "array",
          "items": { "type": "string", "maxLength": 40 },
          "default": [],
          "maxItems": 15,
          "description": "Search terms for this clip, written with its title and description. Carried here rather than only on the channel because they are about THIS clip — a channel-wide list is the same on a goal and on a press conference."
        },
        "review": { "$ref": "#/definitions/ReviewState" },
        "reviewedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "lineageId": {
          "type": ["string", "null"],
          "default": null,
          "description": "The id of the clip this one descends from, at the root of the chain \u2014 the clip RENDER originally made. Every version shares it, so a clip and every correction of it are one row in the review queue instead of five.\n\nThat mattered immediately. The queue lists what is PENDING, a remake is always PENDING, and its parent usually still is too, so a single football clip corrected three times filled four slots and the reviewer had to work out which was newest. Null on a clip written before this field existed; readers treat that as the clip being its own root.",
          "minLength": 1
        },
        "version": {
          "type": "integer",
          "minimum": 1,
          "default": 1,
          "description": "Which attempt this is within its lineage. 1 is the clip RENDER made; a remake is its parent's version plus one. Ordering by this rather than by `createdAt` is deliberate: two remakes of the same parent are siblings, not a sequence, and the number says so."
        },
        "derivedFromClipId": {
          "type": ["string", "null"],
          "default": null,
          "description": "The clip this one was made from, when it is a scored version of another. The original is never altered — a MUSIC job produces a new clip — so this is what relates the two, and what lets the review queue say 'music version of' rather than showing two unexplained near-duplicates."
        },
        "music": {
          "anyOf": [{ "$ref": "#/definitions/AppliedMusic" }, { "type": "null" }],
          "default": null,
          "description": "What was added to this clip, when something was. Null on an ordinary render."
        },
        "remake": {
          "anyOf": [{ "$ref": "#/definitions/AppliedRemake" }, { "type": "null" }],
          "default": null,
          "description": "The correction that produced this clip, when it is one. Null on an ordinary render."
        },
        "reviewNote": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "default": null,
          "description": "What the reviewer thought, in their own words. Distinct from `description`, which is copy that may be published: this is never uploaded anywhere and exists to answer 'why did I reject this?' three weeks later. Phase 9 calibrates the rubric against realised performance; a human's stated reason is the other half of that evidence and is worth capturing while it is fresh."
        },
        "rights": {
          "anyOf": [{ "$ref": "#/definitions/RightsAttestation" }, { "type": "null" }],
          "default": null
        },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "ClipPreview": {
      "title": "ClipPreview",
      "description": "What a phone can actually see when the clip file is not reachable. Stored at clips/{clipId}/preview/poster as base64 — a subcollection document, so the review-queue query does not drag image bytes on every read. Sized to stay well inside Firestore's 1 MiB document limit; at ~40-60 KB the 1 GiB free tier holds roughly 20,000 of these.",
      "type": "object",
      "additionalProperties": false,
      "required": ["clipId", "posterBase64", "widthPx", "heightPx", "createdAt"],
      "properties": {
        "clipId": { "type": "string", "minLength": 1 },
        "posterBase64": {
          "type": "string",
          "minLength": 1,
          "description": "A single representative frame, JPEG, base64-encoded without a data: prefix."
        },
        "filmstripBase64": {
          "type": ["string", "null"],
          "default": null,
          "description": "Four frames tiled into one JPEG, so a reviewer gets a sense of motion rather than a single still."
        },
        "widthPx": { "type": "integer", "minimum": 1 },
        "heightPx": { "type": "integer", "minimum": 1 },
        "byteSize": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "TranscriptRef": {
      "title": "TranscriptRef",
      "description": "What Firestore keeps about a transcript, at sources/{sourceId}/transcripts/{modelVersion}. The word-level segments themselves live on the worker: a 60-minute word-level transcript approaches Firestore's 1 MiB document limit, and the PWA never needs one whole — Candidate.transcriptExcerpt carries the part a reviewer reads.",
      "type": "object",
      "additionalProperties": false,
      "required": ["sourceId", "modelVersion", "localPath", "createdAt"],
      "properties": {
        "sourceId": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "localPath": {
          "type": "string",
          "minLength": 1,
          "description": "Worker-local path to the full Transcript document, as JSON."
        },
        "vadPath": {
          "type": ["string", "null"],
          "default": null,
          "description": "Worker-local path to the voice-activity map produced alongside the transcript: the speech spans, whose gaps are the silences. Phase 5 snaps clip boundaries to those gaps, so it is worth persisting rather than recomputing. Its contents are deliberately NOT modelled here — it never crosses to the PWA, and this document is the wire protocol."
        },
        "language": { "type": ["string", "null"], "default": null },
        "durationSec": { "type": ["number", "null"], "minimum": 0, "default": null },
        "segmentCount": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "wordCount": { "type": ["integer", "null"], "minimum": 0, "default": null },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },

    "PublicationState": {
      "title": "PublicationState",
      "description": "Lifecycle of one attempt to publish one clip to one platform. PENDING is written BEFORE the upload begins, which is what makes a retry reconcile against the platform instead of double-posting.",
      "type": "string",
      "enum": ["PENDING", "UPLOADING", "PUBLISHED", "FAILED", "CANCELLED"]
    },

    "PublishPlatform": {
      "title": "PublishPlatform",
      "description": "Where a clip was published. TikTok and Instagram are out of scope until v0.3+ — both need app review with materially harder approval paths.",
      "type": "string",
      "enum": ["YOUTUBE"]
    },

    "PublishPrivacy": {
      "title": "PublishPrivacy",
      "description": "Defaults to unlisted. Publishing something to the world by accident is not recoverable in the way an unlisted upload is.",
      "type": "string",
      "enum": ["private", "unlisted", "public"]
    },

    "Publication": {
      "title": "Publication",
      "description": "One publish attempt, at clips/{clipId}/publications/{pubId}. Doubles as the audit log: it records who attested the rights basis and on what grounds, so 'who authorised this and why' is answerable for any published clip without reading worker logs.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "clipId", "uid", "platform", "state", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "clipId": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "platform": { "$ref": "#/definitions/PublishPlatform" },
        "state": { "$ref": "#/definitions/PublicationState" },
        "externalId": {
          "type": ["string", "null"],
          "default": null,
          "description": "The platform's id for the uploaded video. Null until the upload completes."
        },
        "externalUrl": { "type": ["string", "null"], "default": null },
        "channelId": {
          "type": ["string", "null"],
          "default": null,
          "description": "Which channel this went to. Recorded on the attempt because a clip published today and re-published elsewhere next month must not look like it went to the same place."
        },
        "privacy": {
          "anyOf": [{ "$ref": "#/definitions/PublishPrivacy" }, { "type": "null" }],
          "default": null
        },
        "title": { "type": ["string", "null"], "default": null },
        "description": { "type": ["string", "null"], "default": null },
        "tags": { "type": "array", "items": { "type": "string" }, "default": [] },
        "categoryId": {
          "type": ["string", "null"],
          "default": null,
          "description": "Recorded because it is part of what went out. The resolved value, not the request's — this record is the answer to 'what did we actually send', and a field that only sometimes reflects the upload answers nothing."
        },
        "rights": {
          "anyOf": [{ "$ref": "#/definitions/RightsAttestation" }, { "type": "null" }],
          "default": null,
          "description": "Copied from the clip at publish time rather than referenced. The attestation that justified THIS upload must survive a later edit to the clip, or the audit trail records the wrong reason."
        },
        "attempts": { "type": "integer", "minimum": 0, "default": 0 },
        "quotaUnits": {
          "type": ["integer", "null"],
          "minimum": 0,
          "default": null,
          "description": "What this attempt cost. A YouTube upload is 1,600 of a 10,000 daily allowance — about six a day — so the budget is tracked rather than discovered on the seventh failure."
        },
        "error": {
          "anyOf": [{ "$ref": "#/definitions/StageError" }, { "type": "null" }],
          "default": null
        },
        "publishAt": {
          "type": ["string", "null"],
          "format": "date-time",
          "default": null,
          "description": "Scheduled publish time. The worker holds the clip until then."
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "publishedAt": { "type": ["string", "null"], "format": "date-time", "default": null }
      }
    },

    "WorkerStatus": {
      "title": "WorkerStatus",
      "type": "string",
      "enum": ["ONLINE", "BUSY", "OFFLINE"]
    },

    "WorkerCapabilities": {
      "title": "WorkerCapabilities",
      "description": "Advertised so the PWA can explain why a job is not progressing, rather than leaving it silently queued.",
      "type": "object",
      "additionalProperties": false,
      "required": ["whisper", "llm", "render", "publish"],
      "properties": {
        "whisper": { "type": "boolean" },
        "llm": { "type": "boolean" },
        "render": { "type": "boolean" },
        "publish": { "type": "boolean" }
      }
    },

    "GpuInfo": {
      "title": "GpuInfo",
      "description": "vramFreeMb is measured through NVML rather than tracked internally, because a foreign process (an interactive Ollama session, say) can hold VRAM the pipeline needs. Observed in Phase 0.",
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "vramTotalMb", "vramFreeMb"],
      "properties": {
        "name": { "type": "string" },
        "vramTotalMb": { "type": "integer", "minimum": 0 },
        "vramFreeMb": { "type": "integer", "minimum": 0 }
      }
    },

    "WorkerHeartbeat": {
      "title": "WorkerHeartbeat",
      "description": "Liveness and capability advertisement at workers/{workerId}.",
      "type": "object",
      "additionalProperties": false,
      "required": ["workerId", "uid", "status", "capabilities", "version", "lastSeenAt"],
      "properties": {
        "workerId": { "type": "string", "minLength": 1 },
        "uid": { "type": "string", "minLength": 1 },
        "status": { "$ref": "#/definitions/WorkerStatus" },
        "capabilities": { "$ref": "#/definitions/WorkerCapabilities" },
        "gpu": {
          "anyOf": [{ "$ref": "#/definitions/GpuInfo" }, { "type": "null" }],
          "default": null
        },
        "version": { "type": "string" },
        "hostname": { "type": ["string", "null"], "default": null },
        "activeJobIds": { "type": "array", "items": { "type": "string" }, "default": [] },
        "lastSeenAt": { "type": "string", "format": "date-time" },
        "startedAt": { "type": ["string", "null"], "format": "date-time", "default": null }
      }
    },

    "AgentDesired": {
      "title": "AgentDesired",
      "description": "What the people want the worker on a machine to be doing. The only thing about an agent the PWA is allowed to say, and it is a wish rather than a fact: the agent decides when it has come true.",
      "type": "string",
      "enum": ["RUNNING", "STOPPED"]
    },

    "AgentState": {
      "title": "AgentState",
      "description": "What the agent has actually managed to do about the wish. FOREIGN means a worker is running on that machine which the agent did not start — from a terminal or the desktop app — which it reports rather than duplicates. FAILED means the worker will not stay up and the agent has stopped retrying; it needs a person, not another restart.",
      "type": "string",
      "enum": ["STOPPED", "STARTING", "RUNNING", "FOREIGN", "STOPPING", "FAILED"]
    },

    "AgentReport": {
      "title": "AgentReport",
      "description": "One machine's supervisor, at agents/{agentId}. The document exists so that starting the worker does not require being at the machine: the PWA writes `desired` from anywhere, including a phone, and the agent — the only party that can spawn a process — writes everything else. Its `lastSeenAt` answers a question no worker heartbeat can, because a worker that is not running cannot say so: an agent beating with state STOPPED means the PC is on and waiting, while an agent that has gone quiet means the PC is off.",
      "type": "object",
      "additionalProperties": false,
      "required": ["agentId", "desired", "state", "version", "lastSeenAt"],
      "properties": {
        "agentId": { "type": "string", "minLength": 1 },
        "hostname": { "type": ["string", "null"], "default": null },
        "version": { "type": "string" },
        "desired": { "$ref": "#/definitions/AgentDesired" },
        "requestedBy": {
          "type": ["string", "null"],
          "default": null,
          "description": "The uid that last asked for a change, so a worker that started on its own is distinguishable from one somebody started."
        },
        "requestedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "state": { "$ref": "#/definitions/AgentState" },
        "detail": {
          "type": ["string", "null"],
          "default": null,
          "description": "One sentence a person can act on, written by the agent. A failure that only exists as an exit code is a failure nobody can diagnose from a phone."
        },
        "workerPid": { "type": ["integer", "null"], "default": null },
        "workerStartedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "lastExitCode": { "type": ["integer", "null"], "default": null },
        "restarts": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "How many times the agent has restarted a worker that died while it was wanted. Reset when a start is asked for, so it counts one bad run rather than the machine's whole history."
        },
        "log": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "The worker's last few output lines, so a start that fails says why on the phone that asked for it. Bounded: a Firestore document is capped at 1 MiB and this one is written every heartbeat."
        },
        "useEmulators": { "type": "boolean", "default": false },
        "projectId": { "type": ["string", "null"], "default": null },
        "startedAt": { "type": ["string", "null"], "format": "date-time", "default": null },
        "lastSeenAt": { "type": "string", "format": "date-time" }
      }
    },

    "LlmClipProposal": {
      "title": "LlmClipProposal",
      "description": "One window as the model returns it. Deliberately carries no total and no precise boundaries: the model supplies judgement, Python supplies arithmetic (D5) and boundary precision (D4).",
      "type": "object",
      "additionalProperties": false,
      "required": ["startSec", "endSec", "subScores", "hook", "reason"],
      "properties": {
        "startSec": { "type": "number", "minimum": 0 },
        "endSec": { "type": "number", "minimum": 0 },
        "subScores": { "$ref": "#/definitions/SubScores" },
        "hook": { "type": "string" },
        "reason": { "type": "string" }
      }
    },

    "LlmClipResponse": {
      "title": "LlmClipResponse",
      "description": "The schema-constrained response from the local model for one transcript window. Passed to Ollama as a format constraint so malformed output is rejected by the runtime rather than parsed defensively here.",
      "type": "object",
      "additionalProperties": false,
      "required": ["clips"],
      "properties": {
        "clips": { "type": "array", "items": { "$ref": "#/definitions/LlmClipProposal" } }
      }
    }
  }
}
